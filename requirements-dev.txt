# requirements-dev.txt
# Development dependencies (not required for running the Streamlit app)

# Load all runtime dependencies
-r requirements.txt

# Testing
pytest

# Code quality / linting
pre-commit
black
flake8

# Optional: test coverage
coverage

# Optional: Airflow for local ingestion / training pipeline
# Airflow is intentionally NOT in requirements.txt because it is heavy (SQLAlchemy, Celery, Gunicorn, etc.)
# Developers who want the weekly pipeline can install it via:
#     pip install -r requirements-dev.txt
apache-airflow==2.9.0