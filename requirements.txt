# requirements.txt
# Runtime dependencies for the Streamlit app (used by Streamlit Cloud)

# UI
streamlit==1.38.0

# Core numerical / ML stack
numpy==1.26.4

# Embeddings / models
sentence-transformers==2.2.2
huggingface-hub==0.25.2

# App / utilities
markdownify==0.14.1
requests==2.32.3
python-dotenv==1.0.1
pyyaml==6.0.2
python-slugify==8.0.4

# OpenAI client (no tiktoken needed on Cloud)
openai==1.12.0
