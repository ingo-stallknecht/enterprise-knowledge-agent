# requirements-dev.txt
# Development dependencies (not required for running the Streamlit app)

# Pull in all runtime dependencies
-r requirements.txt

# Testing
pytest

# Code quality / linting
pre-commit
black
flake8

# Optional: test coverage
coverage

# Optional: Airflow for local ingestion / training pipeline
# This is heavy and intentionally NOT in requirements.txt.
apache-airflow==2.9.0

# Optional: MLflow for experiment tracking, metrics, promotion/rollback
mlflow>=2.0.0
