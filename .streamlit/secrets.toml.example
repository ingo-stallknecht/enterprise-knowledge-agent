# .streamlit/secrets.toml.example
# Backend API endpoint (must be publicly reachable from Streamlit Cloud)
EKA_API = "https://your-fastapi-host.example.com"

# Optional UI tuning
EKA_TIMEOUT_S = "25"
EKA_TOP_K = "12"
EKA_MAX_CHARS = "900"
EKA_RETRIEVAL_MODE = "hybrid"  # dense|sparse|hybrid
EKA_USE_RERANKER = "true"

# NOTE: The Streamlit app does NOT use your OpenAI key directly.
# Your FastAPI server reads OPENAI_* from its own environment/.env.
# If you *do* later run everything in one Streamlit app (not recommended),
# you would add OPENAI_* here and read via st.secrets in server-side code.
